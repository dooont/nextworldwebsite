//really only for reference use
export default class Article{
  constructor(title, source, date, description, link){
    this.title = title;
    this.source = source;
    this.date = date;
    this.description = description;
    this.link = link;
  }
}